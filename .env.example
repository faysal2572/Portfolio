# Environment Variables for Vercel Deployment
# Copy this file to .env.local for local development
# Add these variables in Vercel Dashboard for production

# MongoDB Connection
MONGODB_URI=mongodb+srv://your-username:your-password@cluster.mongodb.net/portfolio?retryWrites=true&w=majority

# Email Configuration (for contact form)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=your-email@gmail.com

# App Configuration
NODE_ENV=production
PORT=3000

# Add your actual values:
# 1. Create a MongoDB Atlas cluster at https://cloud.mongodb.com
# 2. Replace MONGODB_URI with your actual connection string
# 3. Set up email credentials for the contact form
# 4. Add these variables in Vercel Dashboard under Settings > Environment Variables